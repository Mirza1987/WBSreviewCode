@model DigitArchive.Models.BinderType

@{
    ViewBag.Title = "Delete";
}

<h2>Obriši</h2>

<h3>Da li ste sigurni da želite pobrisati ovaj tip registratora? </h3>
<div>
    <h4>Tip registratora</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.BinderTypeName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.BinderTypeName)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Briši" class="btn btn-default" /> |
            @Html.ActionLink("Nazad na listu", "Index")
        </div>
    }
</div>
