@model DigitArchive.Models.Page

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

    <h2>Delete</h2>

<body>
    <h3>Are you sure you want to delete this?</h3>
    <div>
        <h4>Page</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.PageNumber)
            </dt>
    
            <dd>
                @Html.DisplayFor(model => model.PageNumber)
            </dd>
    
            <dt>
                @Html.DisplayNameFor(model => model.PagePath)
            </dt>
    
            <dd>
                @Html.DisplayFor(model => model.PagePath)
            </dd>
    
        </dl>
    
        @using (Html.BeginForm()) {
            @Html.AntiForgeryToken()
    
            <div class="form-actions no-color">
                <input type="submit" value="Obriši" class="btn btn-default" /> |
                @Html.ActionLink("Nazad na listu", "Details", "Documents", new { id = Model.DocumentId }, null)
            </div>
        }
    </div>
</body>
